import React from "react";

import { Layout } from "@/components";
import { GameScene } from "@/scenes";

const Game = () => {
  return (
    <Layout>
      <GameScene />
    </Layout>
  );
};

export default Game;
